#include "main.h"
#include <stdio.h>

/**
 * main -  program that prints its name with a new line
 * @argc: program argument counter
 * @argv: array
 * Return: Always (0)
 */

int main(int argc, char argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
