#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * Return: Always 0
 * @int: Takes an integer
 */

int _abs(int)
{
	int n;

	if (n <= 0)
	{
		_putchar('n' * '-1');
		return (n);
	}

	return (0);
