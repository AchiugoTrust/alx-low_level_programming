#include "main.h"
#include <stdio.h>
/**
 * positive_or_negative - checks if an int is positive or negative
 * @i: Choice integer
 * Return: Always 0
 */
void positive_or_negative(int i)
{
	i = 0;

       	if (i == 0)
	{
		printf("%d is zero", i);	
	}
}
